{
  "name": "client-server-template-with-vite",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && node init.js && lerna clean && yarn && lerna bootstrap",
    "build:client": "lerna run build --scope=client",
    "build:server": "lerna run build --scope=server",
    "build": "yarn build:client && yarn build:server",
    "dev:client": "lerna run dev  --scope=client",
    "dev:server": "lerna run dev --scope=server",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "preview": "yarn build && lerna run preview --parallel",
    "docker": "docker-compose up",
    "docker:preview": "docker-compose up --build",
    "docker:db": "docker-compose up pgadmin -d",
    "docker:dev": "yarn docker:db && yarn dev:server",
    "docker:stop": "docker-compose stop"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=15"
  },
  "devDependencies": {
    "@evilmartians/lefthook": "^1.3.9",
    "lerna": "^5.4.3"
  }
}
