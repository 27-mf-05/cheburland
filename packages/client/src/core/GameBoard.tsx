/*
import { Game } from '@/core/game/Game'

export const GameBoard = () => {
  /!*const {
    gameStatus,
  } = useGameContext();*!/

  return (
    /!*{{gameStatus !== GAME_STATUS.WIN ?
      (
        <div>
              <div>
                <strong>Win :)</strong>
              </div>
              <button onClick={() => this.game.startGame()}>
                Play again
              </button>
        </div>
      ): (
        <>
         <Counter/>
        <Timer/>
        {this.game.startGame()}
        </>

      )}}*!/
    <Game />
  )
}
*/
